# ===========================================
# Ollo's Player Companion — Environment Config
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# 🔑 Gemini API Key (required — server-side only, never exposed to browser)
# Get yours at https://aistudio.google.com/
GEMINI_API_KEY=

# 🔥 Firebase Configuration (required for authentication)
# Create a project at https://console.firebase.google.com/
# Enable Authentication > Sign-in method > Google + Anonymous
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# 🔐 Firebase Admin SDK (required — local dev only)
# On Cloud Run this is auto-detected via the service account.
# For local dev, download a service account key from Firebase Console >
#   Project Settings > Service accounts > Generate new private key
# Then set the path here:
GOOGLE_APPLICATION_CREDENTIALS=

# 🗄️ Redis (optional — enables distributed rate limiting across Cloud Run instances)
# Leave blank to fall back to per-process in-memory rate limiting (fine for single-instance dev).
# In production, use Cloud Memorystore: set REDIS_URL=redis://<private-ip>:6379
REDIS_URL=

# 📚 Gemini File URIs (optional — server-side only, no VITE_ prefix)
# These are auto-populated by running: npm run upload-pdfs
# Place D&D reference PDFs in reference-docs/ first.
# Enables AI answers grounded in actual rulebook text.
GEMINI_FILE_URI_BASIC=
GEMINI_FILE_URI_DMG=
GEMINI_FILE_URI_MM=
GEMINI_FILE_URI_PHB=
